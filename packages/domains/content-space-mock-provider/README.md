# Content Space Local Mock Provider

This trusted compile-time, main-process-only package contributes one provider-neutral
`ContentSpaceProvider` factory and one explicit local Provider Instance. It performs no
network, login, credential, connector, or vendor-adapter work.
